PORT=3000
JWT_SECRET=change-me-in-production-please
DB_PATH=./data/app.db
ADMIN_EMAIL=admin@gym.local
ADMIN_PASSWORD=admin12345

# sync  → command handler awaits notification before responding
# async → command handler returns immediately, notification runs in background
COMMUNICATION_MODE=async
